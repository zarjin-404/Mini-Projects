<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile</title>
  </head>
  <body class="bg-gray-100">
    <div class="container mx-auto p-4">
      <h1 class="text-4xl font-extrabold text-center text-gray-900 mb-8">
        User Profile
      </h1>
      <div class="bg-white p-8 rounded-lg shadow-lg">
        <p class="text-xl font-semibold text-gray-800 mb-3">
          👤: <%= user.name %>
        </p>
        <p class="text-xl font-semibold text-gray-800 mb-5">
          📧: <%= user.email %>
        </p>
        <a
          href="/logout"
          class="bg-blue-700 hover:bg-blue-900 text-white font-semibold py-3 px-6 rounded transition duration-300"
          >Logout</a
        >
      </div>
      <div class="mt-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-4">
          Create Your New Post
        </h1>
        <form
          action="/post"
          method="post"
          class="bg-white p-6 rounded-lg shadow-md"
        >
          <textarea
            name="content"
            class="w-full h-32 p-4 border border-gray-300 rounded-lg mb-4"
            placeholder="Write your post here"
          ></textarea>
          <button
            type="submit"
            class="bg-green-600 hover:bg-green-800 text-white font-semibold py-2 px-4 rounded transition duration-300"
          >
            Post
          </button>
        </form>
      </div>
      <div class="mt-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-4">Your Posts</h1>
        <ul class="space-y-4">
          <% user.posts.reverse().forEach((post) => { %>
          <li class="bg-white p-6 rounded-lg shadow-md">
            <div class="username text-blue-500 font-semibold mb-2">
              @<%= user.name %>
            </div>
            <p class="text-gray-800 mb-2"><%= post.content %></p>
            <div class="flex justify-between items-center mt-4">
              <span><%= post.like.length %></span>
              <a
                href="/like/<%= post._id %>"
                class="text-blue-600 hover:text-blue-800 font-semibold"
                ><%= post.like.indexOf(user._id) > -1 ? "Unlike" : "Like" %></a
              >
              <a
                href="/edit/<%= post._id %>"
                class="text-green-600 hover:text-green-800 font-semibold"
                >Edit</a
              >
            </div>
          </li>
          <% }); %>
        </ul>
      </div>
    </div>
  </body>
</html>
